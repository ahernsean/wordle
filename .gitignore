# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]

# Wordle runtime directory (cache, queue, telemetry, logs, pattern matrix)
wordle_cache_pre_uplift_2026-07-23.sqlite3
runtime/

# Transitory export/import snapshot (stays at the repo root)
wordle_erd_export.sqlite3
wordle_erd_export.sqlite3-shm
wordle_erd_export.sqlite3-wal

# Coverage artifacts
.coverage
.coverage.*
coverage.xml
htmlcov/

# Performance-guard results (written by test_erd_scaling and
# test_swarm_vs_engine_overhead, read by the CI validate steps)
drain_smoke_result.json
strong_scaling_result.json
swarm_overhead_result.json
